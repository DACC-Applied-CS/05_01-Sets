# create a set with names:  Bill, Bob, Tom, Sue

# add the name John

# remove the name Bob

# create a second set with names:  Tom, Sue, Kate

# using a set operation, merge the 2 sets into a new set

# using a set operation, display what names the 2 sets have in common

# using a set operation, display what names are in the
# first set but not the second set

# using a set operation, display what names are in the
# second set but not the first set

# using set comprehension, create and then display 
# a set of the squares of the numbers -5 through 5
